services:
  - type: web
    name: linear-regression-model-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn summative.API.prediction:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
